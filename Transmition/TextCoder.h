#pragma once

#include <cctype>
#include <vector>
#include <string>
#include <iomanip>
#include <iostream>
#include <cstring>

const std::vector<bool>RegistrLet = {1,0,1,0,0,1,0}; 
const std::vector<bool>RegistrDig = {1,0,0,1,0,0,1}; 

std::vector<bool> TextCoder(const char* );